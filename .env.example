# OpenCode Zen API key (https://opencode.ai/zen)
# For local development only. On Streamlit Cloud, put this in
# Settings → Secrets instead — never commit the real key.
OPENCODE_API_KEY=your_opencode_zen_key_here
